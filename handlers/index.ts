import { isVerified } from './isVerified';
import { postFact } from './postFact';
import { postOp } from './postOp';
import { getShortData, getLongData } from './getData';

export { isVerified, postFact, getShortData, getLongData, postOp };
